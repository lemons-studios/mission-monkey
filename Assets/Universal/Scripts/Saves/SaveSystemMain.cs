using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;


// Very much WIP. just created it because I wanted to get started but then got bored and decided to work on other things
public class SaveSystemMain : MonoBehaviour
{
    private string SaveDataLocation = @".\Data\SaveData.json";
    private void LoadData()
    {
        // Write Current Scene and Position to the above specified Json File
    }
    private void SaveData()
    {
        // Load Scene and Position from the json file
    }
}
